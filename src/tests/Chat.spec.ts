import { describe, it, expect } from "vitest";

// TODO: add tests for Chat.vue
describe("basic math", () => {
  it("2 + 2 equals 4", () => {
    expect(2 + 2).toBe(4);
  });

  it("addition is commutative for simple values", () => {
    expect(1 + 3).toBe(4);
  });
});
